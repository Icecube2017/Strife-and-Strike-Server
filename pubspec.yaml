name: sns_server
description: Strife and Strike Server
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  bcrypt: ^1.2.0
  crypto: ^3.0.7
  dart_frog: ^1.1.0
  postgres: ^3.5.11  

dev_dependencies:
  dart_frog_lint: ^0.1.0
  mocktail: ^1.0.0
  test: ^1.0.0
